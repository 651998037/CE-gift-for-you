<%- include("../_header1"); -%>
<br>
<br>
<h1 align="center">หน้าที่</h1>
<div class="container">
    <div class="row mt-5">
        <div class="col-md-15">
            <table class="table table-bordered table-hover"> 
                <thead>
                    <tr>
                        <th>ลำดับที่</th>
                        <th>ชื่อ</th>
                        <th>เพิ่มเติม</th>
                    </tr>
                </thead>
                <a href="/duty/add" class="btn btn-primary">เพิ่มหน้าที่</a>
                <tbody>
                    <% if (data && data.length > 0) { %>
                        <% for (var i = 0; i < data.length; i++) { %>
                            <tr>
                                <td><%= (i + 1) %></td>
                                <td><%= data[i].duty %></td>
                                <td>
                                    <a href="/dutyEdit/<%= data[i].id %>" class="btn btn-success">แก้ไข</a>
                                    <a href="/deleteduty/<%= data[i].id %>" class="btn btn-danger">ลบข้อมูล</a> 
                                </td>
                            </tr>
                        <% } %>
                    <% } %> 
                </tbody>
            </table>
            
        </div>
    </div>
</div>

<%- include("../_footer"); -%>
