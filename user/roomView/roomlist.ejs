<%- include("../_header"); -%>
    <br>
    <br>
    <h1 align="center">ชื่อห้อง</h1>
    <div class="container">
        <div class="row mt-5">
            <div class="col-md-12">
                <table class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>ลำดับ</th>
                            <th>ชื่อห้อง</th>
                            <th>เพิ่มเติม</th>
                        </tr>
                    </thead>
                        <a href="/Uroom/add" class="btn btn-primary">เพิ่มห้อง</a>
                    <tbody>
                        <% if (data && data.length > 0) { %>
                            <% for (var i = 0; i < data.length; i++) { %>
                                <tr>
                                    <td><%= (i + 1) %></td>
                                    <td><%= data[i].roomname %></td>
                                    <td>
                                        <a href="/Uroom/edit/<%= data[i].id %>" class="btn btn-primary">แก้ไข</a>
                                        <a href="/Uroom/delete/<%= data[i].id %>" class="btn btn-danger">ลบข้อมูล</a> 
                                    </td>
                                </tr>
                            <% } %>
                        <% } %> 
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <h1 align="center">ชื่อห้อง</h1>
    <div class="container">
        <div class="row mt-5">
            <div class="col-md-12">
                <table class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>ลำดับ</th>
                            <th>ชื่อห้อง</th>
                        </tr>
                    </thead>
                        <!-- <a href="/Uroom/add" class="btn btn-primary">เพิ่มห้อง</a> -->
                    <tbody>
                        <% if (data && data.length > 0) { %>
                            <% for (var i = 0; i < data.length; i++) { %>
                                <tr>
                                    <td><%= (i + 1) %></td>
                                    <td><%= data[i].roomname %></td>
                                    <td>
                                        <!-- <a href="/Uroom/edit/<%= data[i].id %>" class="btn btn-primary">แก้ไข</a>
                                        <a href="/Uroom/delete/<%= data[i].id %>" class="btn btn-danger">ลบข้อมูล</a>  -->
                                    </td>
                                </tr>
                            <% } %>
                        <% } %> 
                    </tbody>
                </table>
            </div>
        </div>
    </div>


<%- include("../_footer"); -%>